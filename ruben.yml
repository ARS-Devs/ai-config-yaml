version: 1.1.2

cache: false

#interface:
  #presets: false
  #prompts: false

registration:
  socialLogins: ["discord", "facebook", "github", "google", "openid"]
  #allowedDomains:
    #- "advancedrecoverysystems.com"
    #- "recoverykansascity.com"

speech:
  stt:
    openai:
      apiKey: '${STT_API_KEY}'
      model: 'whisper-1'

endpoints:
  assistants:
      disableBuilder: true
      # Use either `supportedIds` or `excludedIds` but not both
      supportedIds: ["asst_KPQ29JpBKZ6W4fG73jDXZxHd"]
      timeoutMs: 780000
  #custom:
